{
  "version": 2,
  "builds": [
    {
      "src": "packages/frontend/apps/web/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "yarn workspace @affine/web run build",
        "outputDirectory": "packages/frontend/apps/web/dist"
      }
    }
  ]
}